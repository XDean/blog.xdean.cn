import React from "react";
import {PostMeta} from "../../domain";

type Props = {
  meta: PostMeta
}
export const PostFooter = (props: Props) => {
  const {} = props
  return (
    <div>
    </div>
  )
}